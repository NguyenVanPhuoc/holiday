<?php
namespace App\Http\Controllers;
use Illuminate\Http\Request;
use App\Http\Requests;
use App\Metas;

class MetaController extends Controller
{       
	/*public function delete(Requests $request){
        if($request->ajax()){
        	$meta = Metas::findBySlug($slug);
        	$meta->delete();
        	return "success";
        }
        return "error";
        exit;
    }*/
}
